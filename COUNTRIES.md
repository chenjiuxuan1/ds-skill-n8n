# 各国 DS 实例与能力矩阵

## 为什么需要这份文档

同一个动作在 A 国能用，**不代表**在 B 国能用。原因有三层，都是真实踩过的坑：

1. **令牌按实例独立存储** —— DS 的令牌表在各国自己的库里，互不相通。拿着 cn 的令牌去打 pk，pk 的 DS 根本不认识它，直接返回 401。
2. **DS 版本不同** —— 巴基斯坦跑的是新版 DS，接口字段和端点名都变了（见下）。
3. **网关部署版本会漂移** —— 每个国家的跳板机各自 checkout 代码仓库，且代码拉取是手动触发的。所以"有没有某个新功能"是按国家、按时间变化的。

> 本文件不含任何内网地址。各国 DS 的实际地址由网关侧的 `config/countries.json` 维护，使用者只需通过 n8n webhook 调用，不需要直连 DS。

## 实例一览

| 国家 | `country` | 令牌来源 | API 形态 | 默认租户 | 默认环境 |
|---|---|---|---|---|---|
| 中国 | `cn` | 中国 DS | 旧版 | `dolphinscheduler` | 无（`-1`） |
| 印尼 | `ine` | 印尼 DS | 旧版 | `default` | 无（`-1`） |
| 墨西哥 | `mx` | 墨西哥 DS | 旧版 | `default` | 无（`-1`） |
| 菲律宾 | `ph` | 菲律宾 DS | 旧版 | `default` | 无（`-1`） |
| 泰国 | `th` | 泰国 DS | 旧版 | `dolphinscheduler` | 有默认环境 |
| **巴基斯坦** | `pk` | 巴基斯坦 DS | **新版** | `default` | 无（`-1`） |

「令牌来源」这一列是重点：**每一列都是一个独立的令牌池**。请到对应国家自己的 DolphinScheduler「安全中心 → 令牌管理 → 新建」创建。

## 最重要的一条：巴基斯坦跑的是新版 DS

新旧两版的关键差异：

| | 旧版（cn / ine / mx / ph / th） | 新版（**pk**） |
|---|---|---|
| 触发用的端点 | `/executors/start-process-instance` | `/executors/start-workflow-instance` |
| 触发表单里的工作流 code 字段 | `processDefinitionCode` | `workflowDefinitionCode` |
| 工作流定义路径 | `workflow-definition` | `workflow-definition`（一致） |

**这对你意味着什么**：通过 n8n 网关调用时，网关会按国家自动适配，你不用管。但任何**绕过网关直连 DS** 的脚本或工具，在巴基斯坦都会挂 —— 它们几乎都写死了旧版字段名。历史上包里就有一个这样的脚本（`n8n/ds_scheduler_router.py`，写死了旧版 API 和个人本机路径），因为无法在巴基斯坦工作且无人引用，已经删除。

## 动作可用性

**网关侧动作（46 个）在六国是一套代码**，与 DS 版本的差异由配置适配，因此不按国家区分可用性。

需要区分的是**部署版本**：

- 网关仓库的 `main` 分支是唯一事实来源；
- 每个国家的跳板机可以落后于 `main`（拉取是手动的）；
- 落后时，**较新加入的动作在该国会表现为"不支持"或字段被忽略**，而不是报一个清楚的错。

所以：**先用 doctor 确认链路，再用一个只读动作确认该国网关的行为符合预期**，然后再执行写操作。

## 开工前先跑 doctor

```bash
python3 scripts/ds_doctor.py --country pk
```

它会依次确认：令牌能否解析 → 令牌格式是否完好（换行、截断）→ 线上 webhook 是否接受该令牌。任何一步失败都会给出下一步该做什么。

典型输出（令牌不属于该国实例时）：

```
  ✅ 解析令牌: 来源 ~/.config/codex-secrets/pk-dolphinscheduler-token，633c…0a，32 位
  ✅ 令牌格式: 格式正常
  ❌ 线上探测: 令牌未通过 pk 的身份校验（DS 401）
      令牌无效，或该令牌不属于 pk 实例。
          DS 令牌存在各国实例自己的库里、互不相通……
```

## 实测记录

| 国家 | 实测时间 | 实测范围 |
|---|---|---|
| `mx` | 2026-09-09 | `create_workflow`、`append_*`、`update_*`、`get_task_log`、`retry_instance`、schedule 上下线 |
| `pk` | 2026-09-18 | 端到端：建工作流 → 改任务重试设置 → 环境切换 → 还原 → 只读动作回归，24 项断言全通过 |

实测是**逐动作**的，不代表该国所有动作都被覆盖过。第一次在某国执行某个写操作前，建议先用测试项目验证。
